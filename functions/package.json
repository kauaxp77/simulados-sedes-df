{
  "name": "simulados-functions",
  "description": "Firebase Functions para projeto de simulados SEDES",
  "scripts": {
    "lint": "eslint .",
    "deploy": "firebase deploy --only functions"
  },
  "engines": {
    "node": "20"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0"
  }
}
